1.Verificar que funcione la aplicación
2.Ingresar a la carpeta del proyecto y crear el archivo Dockerfile
	
	FROM node:18-alpine
	WORKDIR /app
	COPY package*.json ./
	RUN npm install
	COPY . .
	EXPOSE 4200
	CMD ["npm", "start", "--", "--host", "0.0.0.0"]

3.Crear el contenedor de este proyecto:
	
	docker run -d --name factura -p 4000:4200 factura:1



Y ya esta, solo queda subir la imagen.